[build]
  command = "npm run build"
  publish = "dist"

# O app é uma SPA: qualquer rota precisa cair no index.html, senão
# recarregar /materia/quimica devolve 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
